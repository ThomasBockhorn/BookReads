"use strict";

var isNode = require("itsa-utils").isNode,
    win = isNode ? global.window : window;

if (win) {
    require("./lib/polyfill")(win);
    require("./lib/window")(win);
    require("./lib/document")(win);
    require("./lib/element")(win);
    require("./lib/cookies")(win);
    require("./lib/localstorage")(win);
}
